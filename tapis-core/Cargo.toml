[package]
name = "tapis-core"
version = "0.3.1"
authors = ["cicsupport@tacc.utexas.edu"]
description = "Shared traits and types for the Tapis Rust SDK."
license = "BSD-3-Clause"
edition = "2024"
readme = "README.md"

[dependencies]
async-trait = "^0.1"
